import { VerticalImageStackPreview } from "@/components/showcase/demos/vertical-image-stack-preview";

export const metadata = {
  title: "Vertical Image Stack — preview",
};

export default function Page() {
  return <VerticalImageStackPreview />;
}
