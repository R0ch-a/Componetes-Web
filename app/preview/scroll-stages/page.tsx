import { ScrollStagesPreview } from "@/components/showcase/demos/scroll-stages-preview";

export const metadata = {
  title: "Scroll Stages — preview",
};

export default function Page() {
  return <ScrollStagesPreview />;
}
