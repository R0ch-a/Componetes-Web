import { HyperdriveHeroPreview } from "@/components/showcase/demos/hyperdrive-hero-preview";

export const metadata = {
  title: "Hyperdrive Hero — preview",
};

export default function Page() {
  return <HyperdriveHeroPreview />;
}
