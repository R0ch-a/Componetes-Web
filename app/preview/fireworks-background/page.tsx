import { FireworksBackgroundPreview } from "@/components/showcase/demos/fireworks-background-preview";

export const metadata = {
  title: "Fireworks Background — preview",
};

export default function Page() {
  return <FireworksBackgroundPreview />;
}
